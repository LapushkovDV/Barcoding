@model ERP_API_Service.Objects.UserImei

@{
    ViewData["Title"] = "UserImeiDelete";
    ViewBag.Title = "UserImeiDelete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<h2>Удаление</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Удалить IMEI @Html.DisplayFor(model => model.Imei.ImeiCode) у пользователя @Html.DisplayFor(model => model.User.Login)?</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.User.Id)

        <br />
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Удалить" class="btn btn-danger" style="font-size:20px" />
                <a type="button" class="btn btn-light" style="font-size:20px" href="@Url.Action("UserEdit", new { id = Model.User.Id })">Отмена</a>
            </div>
        </div>
        <br />
    </div>
}
